{
  "name": "asymdoom-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --import tsx --test src/test/**/*.test.ts",
    "probe:thin": "node scripts/probe-thinclient.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.13.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
